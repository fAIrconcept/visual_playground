import{LayerCompositor}from'./renderer.js';import{Input}from'./input.js';
const canvas=document.querySelector('#gl'),fps=document.querySelector('#fps'),input=new Input(canvas),app=new LayerCompositor(canvas);await app.loadScene('./scenes/demo/scene.json');let last=performance.now(),yaw=0,ft=0,ff=0;function resize(){const d=Math.min(devicePixelRatio||1,2),w=Math.floor(innerWidth*d),h=Math.floor(innerHeight*d);if(canvas.width!==w||canvas.height!==h){canvas.width=w;canvas.height=h;app.resize(w,h)}}addEventListener('resize',resize);resize();function frame(now){const dt=Math.min((now-last)/1000,.05);last=now;yaw+=input.consumeDX()*.004+.025*dt;app.render({time:now*.001,yaw,pointer:input.pointer});ff++;ft+=dt;if(ft>.5){fps.textContent=`${Math.round(ff/ft)} fps`;ff=0;ft=0}requestAnimationFrame(frame)}requestAnimationFrame(frame);
